name: pipeline_execution_audit
description: Fraction of pipeline runs with immutable execution records capturing
  inputs, parameters, outputs, and completion status
factor: correlated
workload:
- feature-serving
- training
- agents
scope: schema
placeholders:
- database
- schema

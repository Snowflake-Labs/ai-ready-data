name: temporal_referential_integrity
description: Fraction of records with valid, non-null event timestamps traceable to source system origination time
factor: current
direction: gte
workload: [serving, training]
check: sql/check/temporal-referential-integrity.sql
diagnostic: sql/diagnostic/temporal-referential-integrity.sql
scope: table
placeholders: [container, namespace, asset, timestamp_column, key_columns]

name: agent_attribution
description: Fraction of data modifications with a QUERY_TAG identifying the responsible
  agent, pipeline, or process
factor: correlated
workload:
- rag
- feature-serving
- training
- agents
scope: schema
placeholders:
- database
- schema
constraints:
- account_usage.query_history has ~45 minute latency
- user_name is always populated in Snowflake — QUERY_TAG is the meaningful attribution
  signal

# Fix: impact_analysis_capability

No automated fix is available. Impact analysis capability depends on downstream objects (views, tasks, etc.) referencing the base tables. To improve this score, create views or other dependent objects that reference the tables lacking downstream dependents.

name: schema_evolution_tracking
description: Fraction of data assets with automated schema change detection, version
  identifiers, and version history
factor: current
workload:
- training
scope: schema
placeholders:
- database
- schema

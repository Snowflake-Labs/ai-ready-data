name: purpose_limitation
description: Fraction of data access paths with declared permitted AI processing purposes and enforced purpose-based authorization
factor: compliant
direction: gte
workload: [serving, training]
check: sql/check/purpose-limitation.sql
diagnostic: sql/diagnostic/purpose-limitation.sql
fixes:
  - sql/fix/create-tag.sql
  - sql/fix/table-tags.sql
scope: schema
placeholders: [container, namespace, tag_name, tag_value, allowed_values, asset, comment]
constraints:
  - "account_usage.tag_references has ~2 hour latency for new tags"

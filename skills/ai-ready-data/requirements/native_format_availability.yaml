name: native_format_availability
description: Fraction of datasets stored in consumption-ready formats without requiring runtime format conversion
factor: consumable
direction: gte
workload: [serving, training]
check: sql/check/native-format-availability.sql
diagnostic: sql/diagnostic/native-format-availability.sql
scope: schema
placeholders: [database, schema]

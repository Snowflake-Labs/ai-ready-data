name: entity_identifier_declaration
description: Fraction of entities or datasets with explicitly declared primary or natural key definitions
factor: contextual
workload: [rag, feature-serving, training, agents]
check: sql/check/entity-identifier-declaration.sql
diagnostic: sql/diagnostic/entity-identifier-declaration.sql
scope: schema
placeholders: [database, schema]
constraints:
  - "Primary key constraints in Snowflake are not enforced — they are metadata hints"

name: demographic_representation
description: Fraction of training datasets with measured and documented demographic
  distribution relative to target population
factor: compliant
workload:
- training
scope: schema
placeholders:
- database
- schema
- tag_name
- tag_value
- asset
- column
constraints:
- account_usage.tag_references has ~2 hour latency for new tags
- Demographic attributes are sensitive — handle with appropriate access controls

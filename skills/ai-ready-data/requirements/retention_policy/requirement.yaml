name: retention_policy
description: Fraction of datasets with defined and enforced data retention and deletion
  schedules
factor: compliant
workload:
- rag
- feature-serving
- training
- agents
scope: schema
placeholders:
- database
- schema
- tag_name
- tag_value
- allowed_values
- asset
- comment
constraints:
- account_usage.tag_references has ~2 hour latency for new tags
- tag_references has no deleted column — don't filter on it

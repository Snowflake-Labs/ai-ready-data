name: consent_coverage
description: Fraction of personal data records with documented and valid legal basis for AI-specific processing
factor: compliant
direction: gte
workload: [serving, training]
check: sql/check/consent-coverage.sql
diagnostic: sql/diagnostic/consent-coverage.sql
fixes:
  - sql/fix/create-tag.sql
  - sql/fix/table-tags.sql
scope: schema
placeholders: [container, namespace, tag_name, tag_value, allowed_values, asset, comment]
constraints:
  - "account_usage.tag_references has ~2 hour latency for new tags"

name: anonymization_effectiveness
description: Fraction of PII-candidate columns with masking policies applied
factor: compliant
workload:
- training
scope: schema
placeholders:
- database
- schema
constraints:
- account_usage.policy_references has ~2 hour latency
- PII detection is heuristic (column-name based) — delegate to sensitive-data-classification
  for thorough scanning
- Only masking policies count toward the score — privacy tags indicate awareness but
  do not protect data

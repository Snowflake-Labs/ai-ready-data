name: anonymization_effectiveness
description: Fraction of PII-candidate columns with masking policies applied
factor: compliant
workload:
- training
scope: schema
placeholders:
- database
- schema
constraints:
- "PII detection is heuristic (column-name based) \u2014 delegate to sensitive-data-classification\
  \ for thorough scanning"
- "Only masking policies count toward the score \u2014 privacy tags indicate awareness\
  \ but do not protect data"

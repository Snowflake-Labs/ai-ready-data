# Azure Gotchas

Document Azure-specific caveats here as implementations are added.

Examples to capture later:

- metadata propagation between catalog/governance services
- policy enforcement differences across data services
- SQL dialect and function parity constraints
